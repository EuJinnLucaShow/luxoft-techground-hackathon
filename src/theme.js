import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    background: {
      default: '#faf6f9'
    },
    primary: {
      main: '#1F4064',
      contrastText: '#fff'
    },
    secondary: {
      main: '#FFFBD5',
      contrastText: '#fff'
    },
    text: {
      primary: '#000000'
    }
  }
});
